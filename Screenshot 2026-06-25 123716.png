package com.leave.servlet;

import java.io.IOException;
import java.util.List;

import jakarta.servlet.ServletException;
import jakarta.servlet.annotation.WebServlet;
import jakarta.servlet.http.HttpServlet;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;

import com.leave.dao.LeaveDAO;
import com.leave.model.Leave;

@WebServlet("/viewLeaves")
public class ViewLeaveServlet extends HttpServlet {

    protected void doGet(HttpServletRequest request,
                         HttpServletResponse response)
            throws ServletException, IOException {

        LeaveDAO dao = new LeaveDAO();

        List<Leave> leaveList = dao.getAllLeaves();

        request.setAttribute("leaveList", leaveList);

        request.getRequestDispatcher("viewLeaves.jsp")
               .forward(request, response);
    }
}